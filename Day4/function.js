// Do not
// Repeat
// yourself
// NaN -> Not a Number

// untuk mengumpulkan sekelompok urutan program di dalam 1 proses/variable
function sumTwoValues(firstValue, secondValue) {
    let result = firstValue + secondValue;

    console.log(result);
}

// sumTwoValues(23, 94); //cara execute function
// sumTwoValues()

function differenceOfTwoValues(a,b){
    let result = a - b

    console.log(result);
}

const a = 5
const b = 7

differenceOfTwoValues(a,b)